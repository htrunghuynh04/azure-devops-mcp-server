{
  "name": "azure-devops-mcp-server",
  "version": "1.0.0",
  "description": "Remote MCP server bridging Azure DevOps for Claude Cowork",
  "scripts": {
    "prestart": "[ -f node_modules.tar.gz ] && tar -xzf node_modules.tar.gz && rm node_modules.tar.gz || true",
    "start": "node server.js"
  },
  "dependencies": {
    "@azure-devops/mcp": "^2.5.0",
    "supergateway": "^3.4.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
